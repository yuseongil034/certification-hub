# 컴활 2급 실기 핵심 요약

---

## 1. 시나리오

변경셀 · 결과셀 찾기 → **이름정의 먼저** → 시나리오 추가

- 이름정의 안 하면 요약 보고서에 셀 주소로 표시됨 (이름으로 안 나옴)
- 요약 보고서 생성 시 결과셀 범위 정확히 지정할 것
- 변경셀은 여러 개 지정 가능 (Ctrl 클릭)

---

## 2. 데이터 표

결과값 셀에 **=참조 입력 먼저** → 영역 잡기 → 데이터 표 실행

- 영역은 =참조 셀 포함해서 잡을 것
- 행 입력셀 / 열 입력셀 구분
  - 표 상단(가로)에 값 → 행 입력셀
  - 표 왼쪽(세로)에 값 → 열 입력셀

---

## 3. 정렬

조건 **순서대로** 추가 (위쪽이 우선순위 높음)

- ⚠️ 부분합 사용할 거면 **반드시 정렬 먼저**
- 사용자 지정 정렬 시 기준 열 · 정렬 기준 · 순서 세 가지 모두 확인

---

## 4. 부분합

**기준 열 정렬 먼저** → 전체 영역 잘 잡기 → 부분합 실행

- 중첩 시 두 번째부터 **"현재 부분합 대치" 체크 해제**
- 윤곽 기호(1·2·3)로 요약 레벨 조절 가능
- 제거할 땐 **"모두 제거"** 클릭

---

## 5. 통합

영역 잡기 → 입력할 표 & 참조 표 **형태 동일**해야 함

- 형태 다를 경우 → **레이블(행/열 제목) 기준으로 통합 옵션 체크**
- 그래도 다르면 영역을 맞게 조정할 것
- 원본 데이터 연결 체크 시 → 원본 변경되면 결과도 자동 반영
- ⚠️ 참조 범위 추가할 때 **"추가" 버튼** 눌러야 목록에 등록됨

---

## 6. 매크로

- 기록 시작 후 **다른 셀 먼저 클릭**하고 행동할 것
  → 셀 선택 위치까지 매크로에 기록되기 때문
- 데이터는 **미리 입력 후** 매크로 적용할 것
  → 원하는 서식으로 적용하기 쉬움
- 저장 위치는 **현재 통합 문서**로 설정
  → 다른 곳에 저장하면 해당 파일에서 불러와지지 않음
- 단추(버튼)에 연결 시 **양식 컨트롤** 사용 (ActiveX 컨트롤과 혼동 주의)
- 실행 후 결과 확인 필수 → 잘못 기록됐으면 삭제 후 다시 기록

---

## 7. 차트

- 데이터 범위 수정 · 추가 시 범위 잘못 잡지 않도록 조심
- 특정 요소 수정 → **차트 더블클릭(편집모드 진입)** 후 요소 클릭
  → 차트 전체 클릭(1번) vs 편집모드 진입(2번) 구분할 것
- 축 제목, 범례, 데이터 레이블 등 각 요소는 **개별 클릭 후 서식** 적용
- 차트 이동 시 **개체로 이동 vs 새 시트로 이동** 구분
  → 문제에서 시트 지정하면 반드시 확인
- 보조 축 필요 시 해당 계열 클릭 → **데이터 계열 서식 → 보조 축** 선택

---

## 8. 함수

### 📌 기본 규칙

- 수식 다음에 오는 문자는 **무조건 `""` (쌍따옴표)** 로 묶기
- 문자와 문자 사이는 **무조건 `&` (앰퍼샌드)** 로 연결
- 공란(빈 값) 표현은 `""` (띄어쓰기 없는 쌍따옴표)
- ⚠️ 범위가 고정되어야 하는 경우 **반드시 절대참조 (`$`)** 사용
  → 자동채우기(드래그) 시 범위 밀리면 결과 전부 틀림 (F4로 빠르게 고정)

---

### D 함수 (조건 만족 함수)

> DSUM, DAVERAGE, DCOUNT 등

```
D함수(전체범위, 필드, 조건범위)
```

- ⚠️ **유일하게 필드(열 제목)까지 범위에 포함**해서 사용
- 조건범위 구성 방식은 **고급 필터와 동일**

---

### 수학 함수

- **ROUND / ROUNDUP / ROUNDDOWN**: 반올림 · 올림 · 내림
  - 자릿수 양수 → 소수점 아래 / 음수 → 소수점 위
  - ⚠️ 자릿수 잘 보고 판단할 것
- **MOD(수, 나누는수)**: 나머지 반환 → 결과값은 **숫자**

---

### 통계 함수

**RANK.EQ**
```
RANK.EQ(자기자신, 범위, 순서)
```
- 범위는 **반드시 절대참조**
- 순서: 내림차순 → `0` 또는 생략 / 오름차순 → `1`

**LARGE / SMALL**
```
LARGE(범위, k)  /  SMALL(범위, k)
```
- k번째는 숫자 직접 입력 또는 **셀 참조** 모두 가능

---

### 텍스트 함수 (LEFT / MID / RIGHT)

- 결과값은 숫자처럼 보여도 **무조건 문자 취급**
- IF문 등에서 비교할 때 **반드시 `""` 붙이기**
  - 예: `IF(LEFT(A1,1)="3", ...)` ✅
- 몇 개를 제외하고 가져오고 싶을 때 → **LEN에서 빼기(-) 활용**
  - 예: `LEFT(A1, LEN(A1)-2)` → 뒤 2글자 제외하고 가져오기

> ⚠️ MOD 결과는 숫자 / LEFT·MID·RIGHT 결과는 문자 — 혼동 주의!

---

### COUNTIF / SUMIF

**COUNTIF** — 조건을 만족하는 셀 **개수** 세기
```
COUNTIF(범위, 조건)
```

**SUMIF** — 조건을 만족하는 셀의 값 **합계** 구하기
```
SUMIF(조건범위, 조건, 합계범위)
```

> ⚠️ SUMIF는 인수 3개! 조건범위와 합계범위가 다를 수 있음

**예제 데이터**

| 지역 (A열) | 매출 (B열) |
|---|---|
| 서울 | 100 |
| 부산 | 200 |
| 서울 | 150 |

- `COUNTIF(A1:A3, "서울")` → `2` (서울 개수)
- `SUMIF(A1:A3, "서울", B1:B3)` → `250` (서울 매출 합계)
- `SUMIF(A1:A3,"서울",B1:B3) / COUNTIF(A1:A3,"서울")` → `125` (서울 평균)

**조건 작성 규칙**

| 조건 형태 | 작성 방법 | 예시 |
|---|---|---|
| 셀 참조만 단독 | `""` 없이 그냥 참조 | `COUNTIF(A:A, B1)` |
| 문자 직접 입력 | `""` 로 묶기 | `COUNTIF(A:A, "서울")` |
| 비교연산자 + 숫자 | `""` 로 전체 묶기 | `COUNTIF(A:A, ">=10")` |
| 비교연산자 + 셀 참조 | `"연산자"&셀` 로 연결 | `COUNTIF(A:A, ">="&B1)` |

- SUMIF ÷ COUNTIF → **조건부 평균** 계산 가능

---

### 조건 함수

**IF**
```
IF(조건, 참일때 값, 거짓일때 값)
```
- 중첩 IF → 거짓 자리에 계속 IF 삽입

**IFS**
```
IFS(조건1, 값1, 조건2, 값2, ..., TRUE, 기본값)
```
- 마지막에 해당하는 조건 없을 때 → `TRUE` 입력 후 기본값 작성

**IFERROR**
```
IFERROR(수식, 에러일때 표시할 값)
```

---

### CHOOSE / SWITCH

**CHOOSE**
```
CHOOSE(인덱스번호, 값1, 값2, ...)
```
- **숫자 인덱스일 때만 사용 가능**
- 문자 값은 `""` 로 묶기
- ⭐ 함수 마법사(fx) 활용하면 값 채우기 편함

**SWITCH**
```
SWITCH(식, 값1, 결과1, 값2, 결과2, ..., 기본값)
```
- **숫자 · 문자 모두 사용 가능**
- 식과 값이 일치하면 해당 결과 반환

---

### WEEKDAY

```
WEEKDAY(날짜, 형식)
```
- ⚠️ 기본형(형식 생략 또는 1) → **1 = 일요일**, 7 = 토요일

---

### VLOOKUP / HLOOKUP

**VLOOKUP** — 표에서 값을 **세로(열)** 방향으로 찾기
```
VLOOKUP(찾을값, 범위, 열번호, 0)
```

**HLOOKUP** — 표에서 값을 **가로(행)** 방향으로 찾기
```
HLOOKUP(찾을값, 범위, 행번호, 0)
```

**사용 조건**
- ⚠️ 찾을값은 반드시 범위의 **첫 번째 열(VLOOKUP) / 첫 번째 행(HLOOKUP)** 에 있어야 함
- 구하려는 값은 찾을값 기준 **오른쪽(VLOOKUP) / 아래쪽(HLOOKUP)** 에만 가능
- 범위 잡을 때 찾을 열/행이 **맨 왼쪽/맨 위** 에 오도록 잡기

**열번호 / 행번호**
- 범위의 첫 번째 열(행)이 1번
- 원하는 값이 몇 번째 열(행)인지 세서 입력

**마지막 인수**
- `0` → 정확히 일치 (시험에서 거의 항상 0 사용)
- `1` → 근사값 일치 (거의 안 나옴)

**VLOOKUP 예제** — 데이터가 세로로 나열된 경우

| 이름 (A열) | 점수 (B열) |
|---|---|
| 홍길동 | 90 |
| 김철수 | 85 |

→ `VLOOKUP("홍길동", A1:B3, 2, 0)` = `90`  
→ "홍길동"이 첫 번째 열, 점수는 두 번째 열 → 열번호 = **2**

**HLOOKUP 예제** — 데이터가 가로로 나열된 경우

| | A열 | B열 |
|---|---|---|
| 1행 | 홍길동 | 김철수 |
| 2행 | 90 | 85 |

→ `HLOOKUP("홍길동", A1:B2, 2, 0)` = `90`  
→ "홍길동"이 첫 번째 행, 점수는 두 번째 행 → 행번호 = **2**

---

### ✅ 핵심 비교 정리

| 함수 | 결과값 | 비고 |
|---|---|---|
| LEFT / MID / RIGHT | **문자** | 숫자처럼 보여도 문자, `""` 붙이기 |
| MOD | **숫자** | 혼동 주의 |
| CHOOSE | 숫자 인덱스만 | SWITCH는 문자도 가능 |
| VLOOKUP / HLOOKUP | 찾을값 기준 오른쪽(아래) | 첫 열(행)에 찾을값 있어야 함 |
| RANK.EQ 범위 | 절대참조 필수 | 드래그 시 밀림 방지 |


# 컴퓨터활용능력 1급 실기 — 기본작업 핵심 정리

---

## 1. 데이터 관련 기능

### 데이터 불러오기
- **Microsoft Query** 를 통해 외부 데이터 연결

### 고급 필터
- 조건 범위에 **수식을 직접 입력**하여 복잡한 조건 필터링 처리

### 유효성 검사
- 제한 대상(목록·정수·소수·날짜 등) 설정 가능
- 원본에 직접 입력도 가능
- > ⚠ 원본 직접 입력 시 **띄어쓰기 절대 금지**

**와일드카드 패턴**

| 패턴 | 의미 |
|------|------|
| `"글자*"` | 해당 글자로 **시작** |
| `"*글자*"` | 해당 글자 **포함** |
| `"*글자"` | 해당 글자로 **끝** |
| `"*"&A1` | 셀 참조값 포함 (& 로 연결) |

> ⚠ `*`는 반드시 `""` 안에 넣고 `&`로 셀 참조 연결

### 피벗 테이블 — 계산 필드 추가
- 계산 필드 추가는 **별도 탭**에 존재
- 피벗 테이블 도구 → 분석 탭 → 필드/항목/집합 → 계산 필드

---

## 2. 텍스트 / 문자열 함수

### ⚠ LEFT · RIGHT · MID — 반환값은 문자형!
- 세 함수의 결과는 숫자처럼 보여도 모두 **텍스트(문자)**
- 비교할 때 반드시 **따옴표**를 붙여야 올바른 결과가 나옴

```excel
=IF(LEFT(A1,2)="서울", "수도권", "지방")
=IF(MID(B1,3,2)="10", "10월", "기타")
```

> ⚠ 숫자처럼 보여도 반드시 `="01"` 형태로 비교할 것

---

### SEARCH
- 찾을 글자가 원문에서 **몇 번째**에 위치하는지 반환
- 대소문자 구분 없음 / 못 찾으면 오류 반환

```excel
=SEARCH("찾을글자", 원문)
```

---

### LEN
- 문자열의 **길이(글자 수)** 반환

```excel
=LEN(A1)
```

---

### CONCAT / & 연산자
- 문자열을 이어 붙이는 함수 — `&` 연산자와 동일 기능

```excel
=CONCAT(A1, " ", B1)
=A1&" "&B1
```

---

### REPT
- 문자를 지정 횟수만큼 **반복**

```excel
=REPT("★", 3)  →  ★★★
```

---

### REPLACE
- 지정 위치의 글자를 다른 글자로 교체

```excel
=REPLACE(원문, 시작위치, 글자수, 새글자)
=REPLACE("ABCDE", 2, 3, "XX")  →  AXXE
```

---

### SUBSTITUTE
- 특정 문자열을 찾아서 교체

```excel
=SUBSTITUTE(원문, "바꿀글자", "새글자")
=SUBSTITUTE("aabb", "a", "X")  →  XXbb
```

---

### TEXT
- 지정 서식으로 표시 → **결과는 무조건 문자열**

```excel
=TEXT(A1, "yyyy-mm-dd")
=TEXT(1500, "#,##0원")
```

> ⚠ 결과가 문자이므로 수식 계산에 직접 사용 불가

---

### VALUE
- 문자를 **숫자로 변환**

```excel
=VALUE("1500")  →  1500 (숫자형)
```

---

## 3. 통계 / 수학 함수

### AVERAGEA · MAXA · MINA
일반 함수와의 차이점:

| 값 | 처리 방식 |
|----|----------|
| 텍스트 | **0** 으로 포함하여 계산 |
| FALSE | **0** 으로 포함하여 계산 |
| TRUE | **1** 로 포함하여 계산 |

> 💡 AVERAGE는 텍스트 무시, AVERAGEA는 0으로 포함

---

### PERCENTILE
- 범위에서 지정 백분위에 해당하는 **값** 반환

```excel
=PERCENTILE(A1:A10, 0.75)  →  75번째 백분위 값
```

---

### QUOTIENT / MOD

| 함수 | 의미 | 예시 |
|------|------|------|
| `QUOTIENT` | 나눗셈 **몫** | `=QUOTIENT(17,5)` → 3 |
| `MOD` | 나눗셈 **나머지** | `=MOD(17,5)` → 2 |

---

### ROW
- 해당 셀의 **행 번호**를 출력

```excel
=ROW()     →  현재 행 번호
=ROW(A5)   →  5
```

---

### 시간 변환
- 시간은 내부적으로 소수 저장 → **12시 = 0.5**
- 130분을 시간 형식으로 변환:

```excel
= 130 / 60 / 24
→ 셀 서식 [h]:mm 적용 → 2:10 (2시간 10분)
```

---

## 4. 찾기 / 참조 함수

### LOOKUP

```excel
=LOOKUP(찾을값, 찾을범위, 볼값)
```

> 💡 찾을 범위는 오름차순 정렬 필요

---

### INDEX
- 범위에서 지정 위치의 **값을 가져옴**

```excel
=INDEX(범위, 행, 열)
=INDEX(A1:C10, 3, 2)  →  B3 위치의 값
```

---

### XMATCH
- 탐색 방향을 지정할 수 있는 MATCH 함수

| 인수 | 방향 |
|------|------|
| `1` | 위 → 아래 (정방향) |
| `-1` | 아래 → 위 (역방향) |

---

### OFFSET
- 기준 셀에서 이동한 위치의 셀 참조

```excel
=OFFSET(기준셀, 이동행, 이동열)
=OFFSET(A1, 2, 1)  →  B3 참조
```

---

## 5. 조건 / 응용 함수

### D 함수 (데이터베이스 함수)
- 조건 범위를 포함하는 데이터베이스 전용 함수

```excel
=DCOUNT(전체범위, 열번호, 조건범위)
=DSUM(전체범위, 열번호, 조건범위)
```

> 💡 조건 범위에는 필드명 + 조건값 함께 포함

---

### SUMIF / AVERAGEIFS

```excel
=SUMIF(비교범위, "조건", 합계범위)
=AVERAGEIFS(계산범위, 비교범위, 조건)
```

---

### 와일드카드 활용 총정리

| 패턴 | 의미 | 예시 |
|------|------|------|
| `"글자*"` | 시작 글자 | `"김*"` → 김으로 시작 |
| `"*글자*"` | 포함 | `"*서울*"` → 서울 포함 |
| `"*글자"` | 끝 글자 | `"*동"` → 동으로 끝 |
| `"*"&A1` | 셀 참조 포함 | A1 값 포함하는 데이터 |

```excel
=SUMIF(A:A, "*서울*", B:B)
=COUNTIF(A:A, "김*")
=SUMIF(A:A, "*"&C1, B:B)
```

> ⚠ `*`는 반드시 `""` 안에 넣고 `&`로 셀 연결

---

## 6. 배열 수식

### 입력 단축키
- 일반 Enter 대신 **Ctrl + Shift + Enter** 입력
- 수식 입력 후 중괄호 `{}` 자동 생성

> ⚠ 중괄호를 직접 입력하면 배열 수식으로 인식되지 않음

---

### 조건 배열 수식 구조

```excel
=(조건1) * (조건2) * 값
=SUM((A1:A10="서울") * (B1:B10))
```

- 조건 × 조건 = **AND** 효과
- 조건 + 조건 = **OR** 효과

> 💡 논리값(TRUE/FALSE)은 숫자화(×1)해야 COUNT로 셀 수 있음

---

### 계산 순서 주의
- `*`가 `=`(비교) 보다 **먼저** 실행됨

```excel
' 잘못된 예 (괄호 누락)
=(A1=1)+(A2=2)*B1

' 올바른 예
=((A1=1)+(A2=2))*B1
```

---

### 행렬(배열) 표기

```excel
{1,2;3,4}
```

- 쉼표 `,` → 열 구분
- 세미콜론 `;` → 행 구분

---

### FREQUENCY / TRANSPOSE — 영역 먼저 지정 필수!

| 함수 | 설명 |
|------|------|
| `FREQUENCY` | 구간보다 작은 빈도수 반환 |
| `TRANSPOSE` | 행과 열을 바꾸는 함수 |

```excel
=FREQUENCY(데이터범위, 구간범위)
=TRANSPOSE(범위)
```

> ⚠ 두 함수 공통: **결과 영역을 미리 드래그로 지정** → 수식 입력 → Ctrl+Shift+Enter

---

## 7. 재무 함수

### 공통 핵심 규칙
- 모든 재무 함수는 **월(月) 기준** 값 사용
- 연이율 → 월이율: `÷ 12`
- 년 기간 → 월 기간: `× 12`
- 금액 앞에 `-`를 붙여야 **양수 결과** 출력

```
연이율 6%, 3년 → 월이율 = 6%/12 = 0.5%,  월기간 = 3×12 = 36
```

---

### FV (미래가치)
- 매달 저축하면 나중에 얼마가 될까?

```excel
=FV(월이율, 월기간, -금액)
```

---

### PV (현재가치)
- 미래 금액의 현재 가치는 얼마?

```excel
=PV(월이율, 월기간, -금액)
```

---

### PMT (대출 월상환금)
- 대출받으면 매달 얼마씩 갚아야 할까?

```excel
=PMT(월이율, 월기간, -대출금액)
```

---

## 8. 셀 서식 — 사용자 지정 서식

### 기본 구조

```
양수서식 ; 음수서식 ; 0서식 ; 문자서식
```

예시:
```
[빨강]0 ; [파랑]-0 ; [초록]0 ; [노랑]@
```

---

### 색상 지정
```
[빨강]  [파랑]  [초록]  [노랑]  [검정]  [흰색]
```

---

### 조건 포함
```
[>=100][빨강]0
[<0][파랑]-0
[>=100][빨강]▲0 ; [파랑]▼0
```

---

### 기호 의미

| 기호 | 의미 |
|------|------|
| `0` | 자리 강제 표시 (없으면 0 출력) |
| `#` | 숫자 있을 때만 표시 |
| `@` | 텍스트(문자) 표시 |
| `0.0` | 소수 첫째 자리 |
| `0.00` | 소수 둘째 자리 |
| `#,##0` | 천 단위 쉼표 |

---

### 문자 붙이기
```
0"개"       →  5개
#,##0"원"   →  15,000원
0.0"점"     →  87.5점
```

---

### 실전 예시

| 서식 코드 | 결과 | 설명 |
|-----------|------|------|
| `[빨강]▲#,##0;[파랑]▼#,##0;0` | ▲1,500 / ▼200 / 0 | 양수 빨강▲, 음수 파랑▼ |
| `[>=100][빨강]0;[파랑]0` | 100이상 빨강 | 조건별 색상 |
| `#,##0"원";-#,##0"원"` | 1,000원 / -500원 | 단위 표시 |
| `[>=100][빨강]0.0` | 100 이상 → 빨강+소수1자리 | 조건+색상+소수점 |

> ⚠ 셀 서식은 **화면 표시 모양만** 변경 — 실제 저장된 값은 그대로 유지됨

---

## 9. 수식 숨기기 / 시트 보호 / 차트 / 매크로

### 수식 숨기기 순서
1. 셀 서식(Ctrl+1) → 모든 셀 **잠금 해제**
2. 숨기고 싶은 셀만 **다시 잠금 설정**
3. 검토 탭 → **시트 보호** 클릭

> ⚠ 시트 보호를 해야 잠금/숨기기가 실제로 적용됨

---

### 통합문서 보호

| 옵션 | 용도 |
|------|------|
| **구조** | 시트 삽입·삭제·이동·이름 변경 방지 |
| **창** | 일반적으로 **사용 안 함** |

---

### 차트 데이터 추가
1. 차트 먼저 생성 (빈 차트도 가능)
2. 원하는 데이터만 **수동으로 추가**
3. 필요한 항목 반복 추가

---

### 차트 제목 셀 연결
1. 차트 제목 클릭 (선택 상태)
2. 수식 입력줄에 `=` 입력
3. 연결할 셀 클릭 → Enter

> 💡 셀 내용 변경 시 차트 제목도 자동 반영

---

### 매크로 단축키 주의
- 단축키 설정 시 **대문자 입력** → 자동으로 `Shift` 포함됨

> ⚠ 소문자로 입력해야 Shift 없이 단축키 작동

---

## 10. VBA — 사용자 정의 함수

### 함수 선언 기본 구조

```vba
Public Function 함수이름(인수1, 인수2)
    ' 처리 내용
    함수이름 = 반환값
End Function
```

> 💡 함수 이름과 인수는 반드시 지정

---

### IF문 구조

```vba
If 조건 Then
    ' 참일 때 실행
ElseIf 다른조건 Then
    ' 두 번째 조건
Else
    ' 거짓일 때 실행
End If
```

> ⚠ `Then` 키워드 필수 — 없으면 오류

---

### SELECT CASE 구조

```vba
Select Case 변수
    Case Is >= 90
        결과 = "A"
    Case Is >= 80
        결과 = "B"
    Case Else
        결과 = "F"
End Select
```

---

### 전체 예시 — 학점 계산 함수

```vba
Public Function 학점(점수 As Integer) As String
    Select Case 점수
        Case Is >= 90  :  학점 = "A"
        Case Is >= 80  :  학점 = "B"
        Case Is >= 70  :  학점 = "C"
        Case Is >= 60  :  학점 = "D"
        Case Else      :  학점 = "F"
    End Select
End Function
```

> 💡 완성된 함수는 엑셀 셀에서 `=학점(B2)` 형태로 바로 사용 가능
